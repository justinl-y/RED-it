const categoriesInitialState = { categories: undefined };

export default (state = categoriesInitialState, action) => {
  // console.log(state);
  switch (action.type) {
    default:
      return state;
  }
};
